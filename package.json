{
  "name": "@capire/incidents",
  "version": "1.0.0",
  "dependencies": {
    "@cap-js/hana": "^2",
    "@sap/cds": ">=8",
    "@sap/xssec": "^4",
    "express": "^4",
    "ui5-cc-spreadsheetimporter": "^2.3.0"
  },
  "devDependencies": {
    "@cap-js/attachments": "^2",
    "@cap-js/audit-logging": ">=0.8.3",
    "@cap-js/cds-test": "*",
    "@cap-js/change-tracking": "^1.0.6",
    "@cap-js/sqlite": ">=1",
    "@sap/cds-dk": ">=8",
    "cds-plugin-ui5": "^0.13.4"
  },
  "scripts": {
    "watch": "cds watch",
    "start": "cds-serve",
    "jest": "npx jest --silent",
    "test": "chest"
  },
  "sapux": [
    "app/incidents"
  ],
  "private": true,
  "cds": {
    "requires": {
      "[production]": {
        "db": "hana",
        "auth": "xsuaa"
      },
      "destinations": true,
      "html5-runtime": true,
      "workzone": true
    },
    "server": {
      "body_parser": {
        "limit": "10mb"
      }
    }
  }
}
